apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: socket-service          # 변경
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/Parkit-Team/argocd-parkit.git
    targetRevision: main
    path: socket-service        # 변경
  destination:
    server: https://kubernetes.default.svc
    namespace: parkit
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
