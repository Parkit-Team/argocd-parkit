apiVersion: apps/v1
kind: Deployment
metadata:
  name: socket-service        
  namespace: parkit
spec:
  replicas: 1
  selector:
    matchLabels:
      app: socket-service     
  template:
    metadata:
      labels:
        app: socket-service   
    spec:
      containers:
      - name: socket-service  
        image: parkitteam/socket-service:build-86
        ports:
        - containerPort: 8082 
        env:
        - name: SERVER_PORT
          value: "8082"
        - name: SPRINGDOC_SWAGGER_UI_CONFIG_URL
          value: "/socket/v3/api-docs/swagger-config"
        - name: SPRINGDOC_SWAGGER_UI_URL
          value: "/socket/v3/api-docs"
        - name: SPRINGDOC_SWAGGER_UI_DISABLE_SWAGGER_DEFAULT_URL
          value: "true"
        - name: SPRING_KAFKA_BOOTSTRAP_SERVERS
          value: "my-kafka:9092"
