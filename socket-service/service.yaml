apiVersion: v1
kind: Service
metadata:
  name: socket-service        # 변경
  namespace: parkit
spec:
  selector:
    app: socket-service       # 변경
  ports:
  - name: http
    port: 8082
    targetPort: 8082
    nodePort: 30779
  type: NodePort
