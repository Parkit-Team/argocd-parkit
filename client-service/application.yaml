apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: client-service
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/Parkit-Team/argocd-parkit.git
    targetRevision: main
    path: client-service
  destination:
    server: https://kubernetes.default.svc
    namespace: parkit
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
