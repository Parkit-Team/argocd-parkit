apiVersion: v1
kind: Service
metadata:
  name: client-service
  namespace: parkit
spec:
  selector:
    app: client-service
  ports:
  - port: 80
    targetPort: 80
    nodePort: 30080
  type: NodePort
