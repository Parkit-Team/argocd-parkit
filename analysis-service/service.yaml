apiVersion: v1
kind: Service
metadata:
  name: analysis-service
  namespace: parkit
spec:
  selector:
    app: analysis-service
  ports:
  - name: http
    port: 8081
    targetPort: 8081
  type: ClusterIP
