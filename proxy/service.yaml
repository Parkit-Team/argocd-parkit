apiVersion: v1
kind: Service
metadata:
  name: proxy
  namespace: parkit
spec:
  type: NodePort
  selector:
    app: proxy
  ports:
  - name: http
    port: 8080
    targetPort: 8080
    nodePort: 30030
